# Dados do exercício
n1, n2, n3 = 3.6, 8.9, 10
X = 4
N = 3

# Soma das frações
soma_denominador = (1 / (n1 + X)) + (1 / (n2 + X)) + (1 / (n3 + X))

# Aplicando a fórmula completa
H = (N / soma_denominador) - X

print(f"A média harmônica amortizada é: {H:.2f}")

